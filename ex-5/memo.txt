61414050 丹羽直也

コマンドは一通り実装した。エラー処理まで手が回らず、仕様外のパケットを受け取った場合、基本的に切断する仕組みでなんとかごまかした。また、コマンドにファイル名を指定する際に、ダブルクォーテーションで囲むと言った入力を解釈する仕組みを作っていないため、スペースが含まれるファイルやフォルダを指定できない。

なお、バイトオーダーに関して、myftpのヘッダでは変換せずに、例えばx86ならリトルエンディアンのままで格納している。エンド同士のバイトオーダーが一致している限りは問題は起きない。

